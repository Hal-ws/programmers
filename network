def solution(n, computers):
    answer = 0
    visited = [0] * n # 0일때는 방문 안한상태
    for i in range(n):
        computers[i][i] = 0
    for i in range(n):
        for j in range(n):
            if computers[i][j] == 1 and visited[j] != 1:
                q = []


    return answer
